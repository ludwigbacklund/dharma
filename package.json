{
  "name": "dharma",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm run build --recursive",
    "tsc:watch": "tsc -b --watch --preserveWatchOutput",
    "dev": "concurrently --kill-others --names 'TSC,DEV,WATCH' --prefix '({name})' 'pnpm run tsc:watch' 'pnpm run dev --recursive' 'pnpm run watch --recursive'"
  },
  "author": "Ludwig Bäcklund",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^6.3.0",
    "dotenv": "^10.0.0",
    "typescript": "4.4.3"
  }
}
